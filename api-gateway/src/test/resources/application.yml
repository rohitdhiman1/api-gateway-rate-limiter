spring:
  jpa:
    hibernate:
      ddl-auto: validate

gateway:
  jwt:
    # TEST-ONLY secret — never use this value outside of tests
    secret: test-secret-key-that-is-at-least-32-bytes-long-for-hmac
    expiry: PT1H
  routes:
    user-service-url: http://localhost:19999
    pricing-service-url: http://localhost:19998
    notification-service-url: http://localhost:19997

logging:
  level:
    com.gateway: DEBUG
